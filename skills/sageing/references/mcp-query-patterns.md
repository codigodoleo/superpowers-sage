<!-- Loaded on demand from skills/sageing/SKILL.md -->

# MCP Query Patterns

Placeholder reference for MCP query patterns — populated by Onda 5 when the WordPress MCP Adapter + Acorn AI integration ships.

## When Available

When `detect-ai-readiness.mjs` reports `ready: true`, consult this reference before generating code that involves post types, routes, ACF field groups, or Livewire components — query first, generate second.

## Status

Onda 5 prerequisite — this file will be expanded when `/ai-setup` ships.
